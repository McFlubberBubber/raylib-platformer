=========
TODO LIST
=========

- @BUG: Ever since we made the changes to render the game through the texture, there is this weird
		bug that has popped up that might not be related to the point mentioned before, but the
		camera movement for when we reset our player position seems really janky. We may just switch
		the motion to snap since we don't really need to see the camera move from where they died /
		were, to their spawn point, so that could be an easy fix.

- @BUG: Right now, we fixed the weird collision stuff that was occuring with dt spikes, but now we
		have another issue which the input states are messed up. This probably means we need to pull
		out our input handling outside of the update() functions and instead have a whole function
		dedicated to processing the input states in our main loop to ensure things are not latched
		together. Therefore we have the following cycle: input handling -> updating -> drawing.
		
- Always refactor for better architecture, but also get things working first.

- Title the game properly, complete missing main menu features.
- Figure out how to make opening menu + pause menu work with eachother. 

- Get some asset loading system to prepare for importing of character sprites / monster sprites /
  tiles, etc. (just make it work so we can put placeholders in).
- Once we get that sorted, find a good font to use throughout (should probably align with the
  artstyle-vision-thing we have for the game).
- If we do get a sense of direction for the artstyle, we can figure out what to do for the actual
  character sprites, tile map sprites, along with particle systems!

- Think about ways to improve collision detection rather than iterating through the entire array
  of tiles when in the game world. Maybe even switch to raycasting from the player to prevent
  flickering of states in the debug overlay.

- We got the camera system working but we should really be figuring out how we should format our
  world / levels / screens for the long term. We got things working using the screen dimensions,
  but we don't really know if those will continue working once we get the settings page running
  (which will allow different resolution scaling and stuff). Refer to the next section since it
  kinda relates to this one.
  
- Think about how the world editor would work (I had an idea of making it similiar to the
  geometry dash editor, but minus all the triggers, but we could do something like that!).
- Figure out some way we can store the world data to represent it within the game. That way we
  have save the "levels" in some file format somewhere in the game's data folder, and read from that.
- Conventionally, if we did store things in a screen-by-screen format (kinda like Animal Well), then
  we would be able to represent things easily in a simple array of integers, where 0's can be air
  spaces, 1's are tiles, 2's are static spikes, etc.

- We can get our hotloader functionality put into the game, along with some configuration file
  that gets read from / written to to save user config options.

- Clean up some of the console cursor blink handling as we don't automatically do that sometimes.
  For example, in input.cpp, we update the cursor_blink_time manually. 

- We need to do update our memory allocation stuff within the general.cpp because that is what we
  use to heap-allocate stuff so far. Right now, we call out to raylib's MemAlloc() function which
  calls calloc(), but we should consider potentially utilizing platform-specific memory allocation
  such as VirtualAlloc() for Windows, mmap for Linux, etc (not sure what MacOS uses) - mainly
  because people seem to recommend doing so.   -nathan, 23/04/2026

- Since we refactored the world code massively, we need to rebuild our actual world since currently,
  there is no world data (previously, it was hardcoded into a global array). Now, we can figure out
  how to serialize the world data since we have our memory allocation strategies out of the way
  (mostly out of the way). Doing so will allow us to work on the editor and thus make levels pretty
  easily! There is one caveat to all this which is, the current code that exists within the world.cpp
  only considered levels / screens on a horizontal plane, therefore we can't make stuff go high or
  lower, which we should get going to make our platformer more dynamic, rather than a weird side-
  scroller situation.  -nathan, 23/04/2026
